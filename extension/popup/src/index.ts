console.log("popup");

// Create a text node
const textNode = document.createTextNode("Hello, this is a simple text!");

// Append the text node to the textContainer
document.body.appendChild(textNode);
